{
  "name": "news-cli",
  "version": "0.1.0",
  "description": "RSS news aggregation CLI for MBC, JTBC, and DART feeds.",
  "type": "module",
  "bin": {
    "news-cli": "./bin/news-cli.js"
  },
  "scripts": {
    "start": "bun run bin/news-cli.js",
    "build": "bun build ./bin/news-cli.js --compile --outfile dist/news-cli",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "packageManager": "bun@1.3.14",
  "license": "MIT"
}
