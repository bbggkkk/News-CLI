/**
 * Single source of truth for the package version.
 *
 * Centralises the version string so user-agent headers, `--version` output,
 * and other references stay in sync without duplicating magic strings.
 */
export const VERSION = "0.2.8";
